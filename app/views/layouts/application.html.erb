<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for?(:title) ? yield(:title) : 'Rails Girls Submissions App' %></title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap&subset=latin,latin-ext" rel="stylesheet">
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="description" property="og:description" content="Get excited and learn to build the web. Apply for Rails Girls Kraków 2022.">
  <meta property="og:image" content="<%= asset_url('/railsgirls2018.png') %>" />
</head>

</head>
<body class="applications-page">
<p id="notice"><%= notice %></p>

<% if flash[:error] %>
  <div>
    <% flash[:error].each do |error| %>
      <%= error %>
    <% end %>
  </div>
<% end %>

<%= yield %>

<%= render 'layouts/footer' %>
</body>
</html>
